# apps/web
This directory contains the operational control plane built with Next.js (App Router) and Payload CMS.

Responsibilities:
- Authentication & Multi-Tenant Access Control (platform admin, tenant admin, tenant user).
- Dataset file upload handler (CSV, JSON, Parquet).
- Payload Jobs Queue dispatcher.
- Web SQL Query Interface (DuckDB queries).
